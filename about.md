# Pages API Continued

An API to add pages to nodes.

If you are looking to bring back the pages instead of having button clutter then install the Vanilla Pages mod (if available).

As a mod developer, include `PageMenu.h`.

Orignal project by Alphalaneous.